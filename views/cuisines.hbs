{{!-- Citation: CS340 github for node starter app. https://github.com/osu-cs340-ecampus/nodejs-starter-app/tree/main  --}}
{{!Full citation in readme}}

<h1>Cuisines</h1>
<div class="description">
<p>Different categories of cuisines that a restaurant may serve. Cuisines may be viewed, added and deleted.</p>
</div>

<div>
    <h3>Browse or Delete Cuisines</h3>
    {{!-- Create a table --}}
    <table id="cuisine-table">

        {{!-- Header section --}}
        <thead>

            {{!-- For just the first row, we print each key of the row object as a header cell so we
            know what each column means when the page renders --}}
            <tr>
                <th>Cuisine ID</th>
                <th>Description</th>
                <th>Delete</th>
            </tr>
        </thead>

        {{!-- Body section --}}
        <tbody>

            {{!-- For each row, print the following in order --}}
            {{#each data}}
            <tr data-value={{this.cuisineID}}> 
                <td>{{this.cuisineID}}</td>
                <td>{{this.cuisineDescription}}</td>
                <td><button onclick="deleteCuisine({{this.cuisineID}})">Delete</button></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <script src="../public/js/delete_cuisines.js"></script>
    <br>
</div>

<div>
    <h3>Add New Cuisine</h3>

    <form id="add-cuisine-form" method="POST" action="/add-cuisine-form" required>
    <p>Add a new cuisine below. Example: Vegetarian, Thai, Italian, etc.</p>
    <label for="cuisineDescriptionInput">Cuisine Description:</label>
    <input type="text" id="cuisineDescriptionInput" name="cuisineDescriptionInput" required><br>

    
    <input type="submit" value="Add New Cuisine">
    </form>
</div>